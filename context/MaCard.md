Build interactive card game. 

Game Name= Ckrit MaCard
Developer= Jabulani Mdluli

Number of cards= 52
Use SVG images for Cards, Backgrounds (Also PNG/JPG for optional uploaded images)
Game Layout= {
	Top Half of Visible gameplay is DEAL area, if requested card lands here then Dealer wins
	Bottom Half of Visible gameplay is CHALLANGER area, if requested card lands here then Challanger wins
}

Game Objective= {
	Dealer and Challanger must select and agreed to amount of tokens before starting dealing cards, Dealer also start dealing cards into the challanger area (slot etc), also start dealing cards into the dealer area (slot etc), Card Must match Rank Only Unless under Comming soon users that match even suits
	Deal Mode= Shuffle cards every 2 to 3 seconds until Challanger starts making request. Challanger then has following requests/Actions (SmallTop, SmallBottom, SmallCenter, Shuffle) dealer must always follow requests one per time. Dealer finally deals card on table (background), Starting dealing cards 1 by 1 from deck (allow animations with faster button to speed up dealing cards on backround. etc 
	Challanger Mode=Request Actions and select request card, mostly opposite to deal mode }.
	Watch Mode= Able to watch Game can intervene by pressing WIN/LOSS to the requested card

Game Modes= {
	Practice: Offline (Deal Mode / Challenger Mode),
	Reality: Join Game, Create Game,
	Corner: Offline with Bluetooth and/or LocalNet AP, WifiHotSpot
} 

Game Actions = {
	3 or 5 or 7 cards not those specific cards but nuumber of cards
	SmallTop= Take cards from top (roughly 1/4 of total cards) but random number eg. 3 or 5 or 7 cards and put into bottom of deck,
	SmallBottom= Take cards from bottom (roughly 1/4 of total cards) but random number eg. 3 or 5 or 7 cards and put into top of deck,
	SmallCenter= Take cards from center (roughly 1/4 of total cards) but random number eg. 3 or 5 or 7 cards and put into top of deck,
	RequestedCard= Select card Rank for WIN
}

Deal Cards Up down, Not in same line. (Must Be Player Slot and Dealer Slot) to see whether request card is in Dealer or Challenger Area. Optimise Game avoid using libs rather code functions from scratch to avoid load times. Must Be RESPONSIVE, COMPATIBILITY with other browsers refactor dont change game logic. Add Advanced Algorithms. Allow for Customisation of Number of Seconds of shuffle instead of 2, 5 will be default from now on. Add AI Features. Detailed Stats (number win/loses, interactions etc. Use browser APIs, open sources APIs, Polish UI. Game Must Card eSport with Championships. MONETISE APP

Add Advanced Features, Stats, Real-time, Play-through tutorials, Sign Ups, Users Registrations, Minify (everything) remove unused make sure variable/object is initialised before calling it even more allow Guest Mode, Introduce Other Modes, Analysis, Pattern findings, Shuffle Animations, Layouts (Design Different Themes, Setups) Mobile First Approach, Allow Google, Aoth, Facebook etc login Methods, Impliment Bluetooth, Local-net Ap, WiFi Hotspot, Same WiFi, AntiCheat, APIs, secure app. REMEMBER to code most functions from scratch. Try fitting Game OnScreen to avoid scrolling just use Overlays with transparency, enable Accessibility, WPA Standalope config

IMPROVE SVG, Card Backs, Stats Charts, HISTORY, Card Back Builder that Allow image, Allow CACHING, SCALABLE, Bot, Prevention, Open Source and build functions from scratch. Add Other Realtime Chats, Voice Mode, Store as Much User Data as possible (Player Favourite card rank/s) Create TOKEN Named Ckrit TOKEN(NYUKU) for use in even pump.fun. Effects, Animations, Smooth Transactions, Game Modes. Use AI for other features